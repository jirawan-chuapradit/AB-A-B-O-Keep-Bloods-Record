• collection("donateHistory")
	•document(nationalID) // ค่าที่ จนท ค้นหา
		• collection("History")
			•document(date) รับมาจากวันที่บริจาคจริง
--------------------------------------------------------
• collection("user")
	•document(uid) มาจาก String uid = auth.getCurrentUser().getUid();
		•data = ชื่อ-นามสกุล ......
			หมายเลขบัตรประชาชน .....
			กรุ๊ปเลือด ..........
			